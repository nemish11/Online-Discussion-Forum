@model IEnumerable<WebApplication4.Models.Comments>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fourms.title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.commenttext)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.uname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fourms.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.commenttext)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.uname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommentsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommentsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommentsID })
        </td>
    </tr>
}

</table>
